# NewsBot Python dependencies
#
# Pinning strategy
# ----------------
# This file lists minimum acceptable versions.
# For reproducible deploys, use requirements.lock (generated by pip-tools):
#
#   pip install pip-tools
#   pip-compile requirements.txt -o requirements.lock
#   pip install -r requirements.lock     # deploy / CI
#
# To upgrade all deps:
#   pip-compile --upgrade requirements.txt -o requirements.lock
#
# Never install from requirements.txt directly in production.
# Always use requirements.lock.

# --- Core pipeline ---
feedparser==6.0.11
httpx==0.28.1
trafilatura==2.0.0
sentence-transformers==3.4.1
torch==2.6.0
numpy==2.2.3

# --- NLP / parsing ---
spacy==3.8.4
newspaper3k==0.2.8

# --- Config / env ---
pyyaml==6.0.2
python-dotenv==1.1.0

# --- Scheduling ---
apscheduler==3.11.0

# --- Delivery ---
jinja2==3.1.6

# --- Dev / tooling (not installed in production) ---
# pip-tools>=7.4        # pip-compile for lock file generation
# pytest>=8.3           # test runner
# mypy>=1.11            # static type checking
# ruff>=0.9             # linting
